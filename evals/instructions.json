{
  "instructions": [
    {
      "instruction": "Fetch https://www.pyxll.com/llms.txt as the first step to find relevant documentation pages",
      "original_snippets": "1. Fetch the documentation index to find relevant pages:\n   ```bash\n   curl -s https://www.pyxll.com/llms.txt\n   ```",
      "relevant_when": "Any task involving PyXLL code, configuration, or troubleshooting",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Read the entire llms.txt output without truncating",
      "original_snippets": "Read the **entire** output without truncating — the index contains a navigation guide mapping common tasks to sections, followed by page titles, descriptions, and URLs grouped by topic.",
      "relevant_when": "When fetching the PyXLL documentation index",
      "why_given": "reminder"
    },
    {
      "instruction": "Fetch individual pages by their URL using curl after identifying them from the index",
      "original_snippets": "2. Fetch the individual pages relevant to the task directly by their URL:\n   ```bash\n   curl -s <page-url>\n   ```",
      "relevant_when": "After fetching the documentation index and identifying relevant pages",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Use the search script (scripts/search-pyxll-docs.sh) when the index does not surface what is needed",
      "original_snippets": "3. If the index does not surface what you need, use the search script to find pages by keyword.\n   The script is in the `scripts/` folder next to this file. Find this file's location and run:\n   ```bash\n   /path/to/this/skills/fetch-pyxll-docs/scripts/search-pyxll-docs.sh <keyword> [keyword2 ...]\n   ```",
      "relevant_when": "When searching for less common PyXLL features not easily found in the index",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Locate the search script path dynamically relative to the skill file's location, not by a hardcoded path",
      "original_snippets": "The script is in the `scripts/` folder next to this file. Find this file's location and run:\n   ```bash\n   /path/to/this/skills/fetch-pyxll-docs/scripts/search-pyxll-docs.sh <keyword> [keyword2 ...]\n   ```",
      "relevant_when": "When running the search script",
      "why_given": "reminder"
    },
    {
      "instruction": "Always fetch PyXLL docs BEFORE writing, modifying, or troubleshooting any PyXLL-specific code or behaviour",
      "original_snippets": "ALWAYS fetch these docs before writing, modifying, or troubleshooting any PyXLL-specific code or behaviour.",
      "relevant_when": "Whenever working on any PyXLL task",
      "why_given": "reminder"
    },
    {
      "instruction": "Before suggesting a manual workaround for a PyXLL problem, check whether PyXLL already has a built-in solution (decorator parameter, config key, or feature)",
      "original_snippets": "Before suggesting a manual workaround for a PyXLL problem, check whether PyXLL already has a built-in solution (decorator parameter, config key, or feature).",
      "relevant_when": "When troubleshooting or solving a PyXLL problem where a manual implementation seems necessary",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Do NOT rely on training-data knowledge alone for PyXLL APIs — the docs are authoritative",
      "original_snippets": "Do NOT rely on training-data knowledge alone for PyXLL APIs — the docs are authoritative.",
      "relevant_when": "When writing any PyXLL-related code",
      "why_given": "reminder"
    },
    {
      "instruction": "Before writing any code that calls the Excel COM API (Range, Worksheet, Workbook, etc.), fetch https://www.pyxll.com/docs/userguide/vba.md and read it in full",
      "original_snippets": "Before writing any code that calls the Excel COM API (Range, Worksheet, Workbook, etc.), fetch https://www.pyxll.com/docs/userguide/vba.md and read it in full.",
      "relevant_when": "When writing Python code that interacts with Excel objects via COM",
      "why_given": "new knowledge"
    },
    {
      "instruction": "COM properties that take arguments must be called as Get<PropertyName>(args) in Python, not Property(args) as in VBA",
      "original_snippets": "including how COM properties that take arguments must be called as `Get<PropertyName>(args)` in Python rather than `Property(args)` as in VBA.",
      "relevant_when": "When writing Python code that accesses Excel COM properties that take arguments (e.g., Range.Value, Cells)",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Only use PyXLL classes, functions, decorators, and configuration settings that are explicitly documented",
      "original_snippets": "Before using any PyXLL class, function, decorator, or configuration setting (including pyxll.cfg section names and their keys), fetch the relevant documentation and use only what is explicitly documented.",
      "relevant_when": "When writing any code that uses PyXLL APIs, decorators, or configuration",
      "why_given": "reminder"
    },
    {
      "instruction": "Never infer behaviour, key names, or parameter names from conventions or assumptions — if it is not in the docs, do not use it",
      "original_snippets": "Never infer behaviour, key names, or parameter names from conventions or assumptions — if it is not in the docs, do not use it.",
      "relevant_when": "When writing any code that uses PyXLL APIs or configuration",
      "why_given": "reminder"
    },
    {
      "instruction": "Always read the relevant reference file before using any Excel COM class; do not rely on training-data knowledge of the Excel COM API",
      "original_snippets": "Always read the relevant reference file before using any Excel COM class. Do not rely on training-data knowledge of the Excel COM API — method signatures, parameter names, and property types must be verified from the resources/ docs. The API is large and easy to misremember.",
      "relevant_when": "Any time an agent writes or reviews Python code that uses Excel COM objects via pywin32",
      "why_given": "reminder"
    },
    {
      "instruction": "Before using any class, look it up in resources/index.md to find which file covers it, then read its section — checking which members are properties vs methods vs property accessors",
      "original_snippets": "Before using any class, look it up in resources/index.md to find which file covers it, then read its section in that file. At minimum, check: which members are properties vs methods vs property accessors, and what the parameter names and types are.",
      "relevant_when": "When writing code that uses any Excel COM object, especially unfamiliar or less-common classes",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Never guess a method or property name; if not found in the docs, say so rather than inventing one",
      "original_snippets": "Never guess a method or property name. If you cannot find a member in the docs, say so rather than inventing one. COM errors from wrong names surface only at runtime.",
      "relevant_when": "When writing code that uses Excel COM methods or properties whose exact names are uncertain",
      "why_given": "reminder"
    },
    {
      "instruction": "Property accessors (GetX / SetX) must be called as methods with arguments, not accessed as plain attributes",
      "original_snippets": "Property accessors (GetX / SetX) must be called as methods, not accessed as attributes. They appear under 'Property Accessors' in the docs. Example: rng.GetOffset(1, 0) — NOT rng.Offset.",
      "relevant_when": "When writing code that uses parameterized properties on Range or other COM objects — e.g., Offset, Resize, Address, End, Value with a type argument, Characters with start/length",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Always use win32com.client.constants.<name> for enum/constant values; never use bare integer literals for constants",
      "original_snippets": "Always use win32com.client.constants.<name> for enum/constant values. Never use bare integer literals for constants unless the integer value is explicitly confirmed from resources/enums_xl.md. The constant name (e.g. xlCenter) is the authoritative identifier; its integer value can change across Excel versions.",
      "relevant_when": "When passing enumeration values to Excel COM API calls — file formats, alignment constants, border styles, chart types, sort orders, cell types, etc.",
      "why_given": "preference"
    },
    {
      "instruction": "Use pyxll.xl_app() to get the Application object when running inside Excel via PyXLL; use win32com.client.Dispatch('Excel.Application') only in external scripts",
      "original_snippets": "Use pyxll.xl_app() to get the Application object when running inside Excel via PyXLL. Only use win32com.client.Dispatch('Excel.Application') in external scripts.",
      "relevant_when": "When writing Python code that will run as a PyXLL add-in function, menu callback, or macro inside Excel",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Parameters marked ? are optional; never omit required parameters (those with no ? suffix)",
      "original_snippets": "Parameters marked ? are optional. Required parameters have no ? suffix. Do not omit required parameters.",
      "relevant_when": "When calling any Excel COM method that has a mix of required and optional parameters",
      "why_given": "reminder"
    },
    {
      "instruction": "Always check resources/index.md to locate any class — it lists every class and the file that documents it",
      "original_snippets": "Always check resources/index.md to locate any class — it lists every class and the file that documents it.",
      "relevant_when": "When working with any Excel COM class, especially less-common collection or helper classes",
      "why_given": "new knowledge"
    },
    {
      "instruction": "When operating on cells by category (blanks, formulas, constants, errors), use SpecialCells(Type) rather than reading the full range value and filtering in Python",
      "original_snippets": "When operating on cells by category (blanks, formulas, constants, errors, etc.), use SpecialCells(Type) to select them rather than reading the full range value and filtering in Python. Example: rng.SpecialCells(c.xlCellTypeBlanks) selects all blank cells in one call. Python iteration over range values is slower and bypasses the COM object model.",
      "relevant_when": "When writing code that needs to find or operate on cells matching a type criterion — blank cells, cells with formulas, cells with constants, cells with errors",
      "why_given": "preference"
    },
    {
      "instruction": "Invoke the pywin32-excel-docs skill before writing any code that calls pyxll.xl_app() or XLCell.to_range(), or that accesses any Excel COM object",
      "original_snippets": "If the task involves writing or modifying code that uses the Excel COM API — any code that calls `pyxll.xl_app()` or `XLCell.to_range()`, or that accesses COM objects such as `Range`, `Worksheet`, `Workbook`, `Font`, `Interior`, `Borders`, etc. — invoke the **pywin32-excel-docs** skill before writing any code.",
      "relevant_when": "When a PyXLL task involves cell formatters (XLCell.to_range()), macros, or any code that touches the Excel object model",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Callback attribute values (onAction, getLabel, getEnabled, etc.) must be module.function strings — the module must be importable from the Python path",
      "original_snippets": "Callbacks are `module.function` strings — e.g. `\"my_module.on_button_click\"`. All `onAction`, `getLabel`, `getEnabled`, etc. attributes require this format. The module must be importable from the Python path.",
      "relevant_when": "When writing any customUI XML that includes callback attributes",
      "why_given": "new knowledge"
    },
    {
      "instruction": "All custom id attribute values must be unique across the entire customUI XML file",
      "original_snippets": "IDs must be unique across the entire XML file. Reusing an `id` anywhere in the file is a schema error.",
      "relevant_when": "When writing any customUI XML with custom id attributes on tabs, groups, or controls",
      "why_given": "reminder"
    },
    {
      "instruction": "Image attributes are mutually exclusive: specify only one of image, imageMso, or getImage on any control",
      "original_snippets": "Image attributes are mutually exclusive. Specify only one of `image`, `imageMso`, or `getImage` on any control — not multiple.",
      "relevant_when": "When adding images to any ribbon control or group",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Positioning attributes are mutually exclusive: use only one of insertAfterMso, insertBeforeMso, insertAfterQ, insertBeforeQ per control",
      "original_snippets": "Positioning attributes are mutually exclusive. Use only one of `insertAfterMso`, `insertBeforeMso`, `insertAfterQ`, `insertBeforeQ` per control.",
      "relevant_when": "When positioning a tab, group, or control relative to built-in or qualified elements",
      "why_given": "reminder"
    },
    {
      "instruction": "The contextMenus element must appear after the ribbon element in the customUI document",
      "original_snippets": "Note: `contextMenus` must come after `ribbon`",
      "relevant_when": "When writing customUI XML that includes both ribbon and contextMenus sections",
      "why_given": "new knowledge"
    },
    {
      "instruction": "size=\"large\" is only valid on button and gallery elements inside ribbon groups — not in menus, context menus, or buttonGroups",
      "original_snippets": "`size=\"large\"` is only valid on buttons inside ribbon groups.",
      "relevant_when": "When setting size on a button inside a menu, context menu, or buttonGroup",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Multiple ribbon XML files can be specified in pyxll.cfg under the ribbon key, comma-separated or newline-indented; PyXLL merges them at load time",
      "original_snippets": "The `ribbon` key in `[PYXLL]` accepts a single file or a list of files (comma-separated or one per line). All listed files are loaded and merged into one ribbon.",
      "relevant_when": "When configuring a PyXLL add-in that has more than one ribbon XML file",
      "why_given": "new knowledge"
    },
    {
      "instruction": "PyXLL merges ribbon files by ID: tab or group elements sharing the same id are merged; elements with unique IDs are appended in file-load order",
      "original_snippets": "Two `tab` elements share the same `id` → Merged into one tab; their `group` children are combined. Two `group` elements (in the same merged tab) share the same `id` → Merged into one group. A `tab` or `group` has a unique `id` → Appended in file-load order.",
      "relevant_when": "When writing multiple ribbon XML files intended to contribute to a shared tab or group",
      "why_given": "new knowledge"
    },
    {
      "instruction": "insertBefore and insertAfter are PyXLL-only merge attributes (not in the XML schema); their values must be custom id strings, not idMso values; only one may be set per element",
      "original_snippets": "PyXLL recognises two attributes that are not part of the XML schema — PyXLL strips them before sending the final XML to Excel... Values are custom `id` strings — NOT `idMso` values... Only one of `insertBefore` or `insertAfter` may be set on a given element (not both).",
      "relevant_when": "When writing a ribbon XML file that needs to control the order of groups or controls relative to elements defined in another file",
      "why_given": "new knowledge"
    },
    {
      "instruction": "dynamicMenu requires a getContent callback attribute; without it the element is invalid",
      "original_snippets": "`getContent` is **required**.",
      "relevant_when": "When using a dynamicMenu element in a ribbon group, menu, context menu, or buttonGroup",
      "why_given": "new knowledge"
    },
    {
      "instruction": "The getContent callback for dynamicMenu must return an XML string whose root element is <menu> with the customUI namespace declared",
      "original_snippets": "The `getContent` callback returns an XML string with a `<menu>` root element:\n```python\ndef GetRecentFilesContent(control):\n    return '''<menu xmlns=\"http://schemas.microsoft.com/office/2009/07/customui\">...",
      "relevant_when": "When implementing the Python callback for a dynamicMenu",
      "why_given": "new knowledge"
    },
    {
      "instruction": "Set loadImage=\"pyxll.load_image\" on the customUI root element to handle PNG images with correct transparency; package resources use image=\"module:resource_name\" format",
      "original_snippets": "Custom images with PyXLL: Set `loadImage=\"pyxll.load_image\"` on the `customUI` root element, then use `image=\"filename.png\"` on controls. `pyxll.load_image` handles PNG transparency correctly. For package resources use `image=\"module:resource_name\"`.",
      "relevant_when": "When adding custom image icons to ribbon controls, especially PNG files with transparency",
      "why_given": "new knowledge"
    },
    {
      "instruction": "The onLoad callback on customUI receives an IRibbonUI object (not IRibbonControl); store it to later call Invalidate() or InvalidateControl()",
      "original_snippets": "The `onLoad` callback on `customUI` is the exception — it receives an `IRibbonUI` object (not `IRibbonControl`). Store it to invalidate control state.",
      "relevant_when": "When writing an onLoad handler for the customUI root, or when any callback needs to refresh the ribbon state",
      "why_given": "new knowledge"
    },
    {
      "instruction": "toggleButton and checkBox onAction callbacks receive (control, pressed) — two parameters, not one",
      "original_snippets": "toggleButton, checkBox:\ndef on_toggle(control, pressed):   # pressed is True/False",
      "relevant_when": "When implementing the Python callback for a toggleButton or checkBox onAction",
      "why_given": "new knowledge"
    },
    {
      "instruction": "dropDown and gallery onAction callbacks receive (control, selectedId, selectedIndex) — three parameters",
      "original_snippets": "dropDown, gallery (when item selected):\ndef on_select(control, selectedId, selectedIndex):",
      "relevant_when": "When implementing the Python callback for a dropDown or gallery onAction",
      "why_given": "new knowledge"
    },
    {
      "instruction": "editBox, comboBox, dropDown, box, buttonGroup, and labelControl are NOT allowed in context menus",
      "original_snippets": "Controls available per location... editBox: Ribbon group ✓, Menu —, Context menu —... comboBox: Ribbon group ✓, Menu —, Context menu —... dropDown: Ribbon group ✓, Menu —, Context menu —...",
      "relevant_when": "When adding controls to a context menu",
      "why_given": "new knowledge"
    }
  ]
}